shader_type canvas_item;

uniform vec4 background_color : source_color;
uniform vec2 offset_in_pixels;

void shader_type(in vec3 x) {
    4;
    switch (x) {
        case 1:
            break;
        default:
            break;
    }
}
